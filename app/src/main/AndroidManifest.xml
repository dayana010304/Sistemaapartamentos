<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sistema.apartamentos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/inicie"
        android:label="@string/app_name"
        android:roundIcon="@drawable/inicie"
        android:supportsRtl="true"
        android:theme="@style/Theme.SistemaApartamentos">
        <activity android:name=".editUserInvitedActivity"></activity>
        <activity android:name=".editUserActivity" />
        <activity android:name=".InvitedActivity" />
        <activity android:name=".perfiluserActivity" />
        <activity android:name=".EditAparmentActivity" />
        <activity android:name=".ListApartmentActivity" />
        <activity android:name=".RegisterAparmentActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>